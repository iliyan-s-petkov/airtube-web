CLAUDE.md

/airbg
*.pmtiles
# ...except the miniature fixture the tiles handler's tests serve. It is 25
# bytes of source, not a generated archive; the handler never parses it.
!internal/tiles/testdata/bulgaria.pmtiles
.env
.superpowers/

# Local Claude Code settings — personal, not team configuration.
.claude/

# Vite output. Built by `npm run build` in web/, embedded by internal/web.
# .keep is committed so //go:embed always has a match — see the 3a spec §4.2.
internal/web/dist/*
!internal/web/dist/.keep
web/node_modules/
web/test-results/
web/playwright-report/
web/blob-report/

# Scratch from tooling, not artefacts: an empty vite cache npm leaves in the
# repo root, and the browser-automation MCP's captures. Untracked was not
# enough — `git add -A` would have committed node_modules wholesale.
node_modules/
.playwright-mcp/
audit-home-full.png
sofia-poi-zoom.png
node-compile-cache/
.claude-flow/
