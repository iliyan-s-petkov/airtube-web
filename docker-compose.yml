# Local development only. Do NOT use this compose file in production: it
# publishes Postgres on the host (port 5432), ships a plaintext password in
# plain environment variables, and carries none of the operational hardening
# (secrets management, backups, network isolation, resource limits) a
# production deployment needs.
services:
  db:
    image: timescale/timescaledb-ha:pg16
    environment:
      POSTGRES_PASSWORD: airbg
      POSTGRES_USER: airbg
      POSTGRES_DB: airbg
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U airbg"]
      interval: 5s
      timeout: 5s
      retries: 10

  collector:
    build: .
    command: ["collect"]
    environment:
      AIRBG_DATABASE_URL: postgres://airbg:airbg@db:5432/airbg?sslmode=disable
    depends_on:
      db:
        condition: service_healthy
    read_only: true
    cap_drop: ["ALL"]
    security_opt: ["no-new-privileges:true"]
