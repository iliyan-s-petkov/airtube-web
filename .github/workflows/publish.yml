name: publish
on:
  push:
    branches: [master]
    tags: ['v*']
  pull_request:
permissions:
  contents: read
  packages: write
  id-token: write
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7
      - uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f  # v3
      - if: github.event_name != 'pull_request'
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9  # v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - id: tag
        run: echo "short=$(git rev-parse --short=7 HEAD)" >> "$GITHUB_OUTPUT"
      - id: build
        if: github.event_name != 'pull_request'
        uses: docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8  # v6
        with:
          context: .
          platforms: linux/amd64
          push: true
          provenance: mode=max
          sbom: true
          tags: ghcr.io/iliyan-s-petkov/airbg:${{ steps.tag.outputs.short }}
          labels: org.opencontainers.image.source=https://github.com/iliyan-s-petkov/airtube-web
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - id: build-pr
        if: github.event_name == 'pull_request'
        uses: docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8  # v6
        with:
          context: .
          platforms: linux/amd64
          push: false
          load: true
          tags: ghcr.io/iliyan-s-petkov/airbg:pr-check
          labels: org.opencontainers.image.source=https://github.com/iliyan-s-petkov/airtube-web
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25  # v0.36.0
        with:
          image-ref: ${{ github.event_name == 'pull_request' && 'ghcr.io/iliyan-s-petkov/airbg:pr-check' || format('ghcr.io/iliyan-s-petkov/airbg@{0}', steps.build.outputs.digest) }}
          severity: CRITICAL,HIGH
          ignore-unfixed: true
          exit-code: '1'
          vuln-type: os,library
      - if: github.event_name != 'pull_request'
        uses: sigstore/cosign-installer@398d4b0eeef1380460a10c8013a76f728fb906ac  # v3
      # Trivy runs before cosign on purpose: a scan failure leaves the pushed image unsigned, and 7.5's verify step refuses unsigned images.
      - if: github.event_name != 'pull_request'
        run: cosign sign --yes ghcr.io/iliyan-s-petkov/airbg@${{ steps.build.outputs.digest }}
      - if: github.event_name != 'pull_request'
        env:
          DIGEST: ${{ steps.build.outputs.digest }}
          REF_NAME: ${{ github.ref_name }}
        run: |
          tags=(-t "ghcr.io/iliyan-s-petkov/airbg:latest")
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            tags+=(-t "ghcr.io/iliyan-s-petkov/airbg:$REF_NAME")
          fi
          docker buildx imagetools create "${tags[@]}" "ghcr.io/iliyan-s-petkov/airbg@$DIGEST"
